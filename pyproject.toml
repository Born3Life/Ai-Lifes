[project]
name = "neuro-guide"
version = "0.1.0"
description = "AI-контент для TG и VK — утро, день, вечер"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.1",
    "Pillow>=11.0.0",
]

[project.scripts]
neuro-guide = "scheduler:main"
